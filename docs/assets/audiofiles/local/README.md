# Lokale Audio-Assets (nicht versioniert)

Lege hier Audio-Dateien ab, die nur lokal vorliegen und manuell auf neue Rechner migriert werden.

Dieser Ordner bleibt gitignored. Diese README bleibt versioniert als Platzhalter...